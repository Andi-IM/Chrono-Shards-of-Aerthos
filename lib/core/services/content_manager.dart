import 'package:csoa/core/models/enemy_data.dart';
import 'package:csoa/core/models/item_data.dart';

class ContentManager {
  // Logika untuk memuat data item dari file JSON.
  ItemData getItem(String id) {
    // Implementasi logika belum ada.
    throw UnimplementedError();
  }

  // Logika untuk memuat data musuh dari file JSON.
  EnemyData getEnemy(String id) {
    // Implementasi logika belum ada.
    throw UnimplementedError();
  }
}
